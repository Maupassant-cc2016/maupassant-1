<div class="page-navigator">
    {{#if prev}}
        <a class="pre" href="{{page_url prev}}">&larr; Newer Posts</a>
    {{/if}}
    {{#if next}}
        <a class="next" href="{{page_url next}}">Older Posts &rarr;</a>
    {{/if}}
</div>